name: Build image and open dev GitOps PR

on:
  push:
    branches:
      - main
  workflow_dispatch:

permissions:
  contents: read
  packages: write

env:
  APP_NAME: gitops-demo
  IMAGE_NAME: ghcr.io/mint-cocoa/mintcocoa-gitops-demo-app
  OPS_REPO: mint-cocoa/mintcocoa-ops

jobs:
  build-and-propose-dev:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout application repository
        uses: actions/checkout@v4

      - name: Set image tag
        run: |
          set -euo pipefail
          echo "IMAGE_TAG=${GITHUB_SHA}" >> "$GITHUB_ENV"
          echo "IMAGE=${IMAGE_NAME}:${GITHUB_SHA}" >> "$GITHUB_ENV"

      - name: Log in to GHCR
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Set up QEMU
        uses: docker/setup-qemu-action@v3

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Build and push image
        uses: docker/build-push-action@v6
        with:
          context: .
          push: true
          platforms: linux/arm64
          build-args: |
            APP_VERSION=${{ github.sha }}
          tags: ${{ env.IMAGE }}

      - name: Checkout GitOps repository
        uses: actions/checkout@v4
        with:
          repository: ${{ env.OPS_REPO }}
          token: ${{ secrets.OPS_REPO_TOKEN }}
          path: mintcocoa-ops

      - name: Open dev overlay PR
        working-directory: mintcocoa-ops
        env:
          GH_TOKEN: ${{ secrets.OPS_REPO_TOKEN }}
        run: |
          set -euo pipefail
          file="apps/${APP_NAME}/overlays/dev/kustomization.yaml"
          branch="dev/${APP_NAME}-${IMAGE_TAG}"

          sed -i "s|newName: .*|newName: ${IMAGE_NAME}|" "$file"
          sed -i "s|newTag: .*|newTag: ${IMAGE_TAG}|" "$file"

          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git checkout -B "$branch"
          git add "$file"

          if git diff --cached --quiet; then
            echo "dev overlay already uses ${IMAGE}; no PR needed."
            exit 0
          fi

          git commit -m "Update ${APP_NAME} dev image to ${IMAGE_TAG}"
          git push --force-with-lease origin "$branch"

          body="Updates \`${file}\` to \`${IMAGE}\`. Merge this PR to let Argo CD sync the dev/home k3s overlay."
          pr_number="$(gh pr list --repo "$OPS_REPO" --head "$branch" --state open --json number --jq '.[0].number // empty')"

          if [ -n "$pr_number" ]; then
            gh pr edit "$pr_number" \
              --repo "$OPS_REPO" \
              --title "Update ${APP_NAME} dev image to ${IMAGE_TAG}" \
              --body "$body"
          else
            gh pr create \
              --repo "$OPS_REPO" \
              --base main \
              --head "$branch" \
              --title "Update ${APP_NAME} dev image to ${IMAGE_TAG}" \
              --body "$body"
          fi
